CREATE TABLE `Ulogin` (
	`UID` INT(32) NOT NULL AUTO_INCREMENT,
	`Username` VARCHAR(50) NOT NULL,
	`Password` VARCHAR(50) NOT NULL,
	PRIMARY KEY (`UID`)
);

INSERT INTO 'Ulogin' (IUD, Username, Password)
VALUES (1, 'Big', 'Boy', );